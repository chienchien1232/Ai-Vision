package com.example.ai_vision

// Shared defaults for new features. Existing ViewModel timeouts remain unchanged.
object AppConfig {
    const val CONNECTION_TIMEOUT_MS = 5_000L
    const val COMMAND_TIMEOUT_MS = 3_000L
}
